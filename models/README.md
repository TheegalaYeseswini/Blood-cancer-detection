This folder stores the trained model weights used by the app.

- `blood_cancer.pth`
  Broad tetra classifier
- `lukemia_sub.pth`
  Leukemia subtype classifier
- `lymphoma_sub.pth`
  Lymphoma subtype classifier

If you rename these files again, update `get_default_model_configs()` in `src/load_models.py`.
